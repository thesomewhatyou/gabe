import Base64Command from "./utility/base64.js";

class UtilityCommand extends Base64Command {
    static description = "Utility commands for various tasks";
    static aliases = ["util", "utils"];
}

export default UtilityCommand;
